# Ensure Git does not change line endings by default
* -text

# Force CRLF for PowerShell scripts
*.ps1 text eol=crlf

# Optionally, you can force LF for Bash and Python scripts
*.sh text eol=lf
*.py text eol=lf
